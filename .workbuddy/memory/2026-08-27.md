# 2026-08-27 工作日志

## 项目体检：码上Python (PyNeon / PY//NOW) v0.3.3

**定位**：赛博朋克风、完全离线、内嵌 CPython 3.13 的安卓 Python 学习终端。
技术栈：Kotlin+Compose(M3) / AGP 8.7.3 / Gradle 8.10.2 / Chaquopy 16.0.0 / Room 2.6.1 / DataStore 1.1.1 / ZXing(证书海报)。

**已验证事实（实测证据）**：
- 桌面引擎单测 `tests/test_engine_desktop.py`：16 passed（anaconda python 3.x + pytest 9.1.1）。
- 内容校验 `tests/validate_content.py`：课程 30（assets 29 + content_packs/bonus-builtin.json 1）+ 挑战 6，判题项 36，用例 130，结构问题 0，判题失败 0。
- Kotlin↔Python 桥接 `PyBridge.kt` 与 `runner.py`/`repl.py` 接口契约一致（run_code_json / check_exercise_json / repl start·push / info_json，字段对齐）。
- 课程数真相：3 个 assets JSON 实际 10+8+11=29，第 30 课来自彩蛋包 bonus-builtin.json（v0.2.1 内置函数巡礼）。README「30 讲」正确。

**发现的问题 / 待办**：
1. **PLAN.md 严重落后于代码**（doc drift）：仍写「本次 M0–M3 / 10 课 / 7 界面」，实际已是 30 课 + 内容中心/证书/错题本/连击等约 12 个 screen，versionName 0.3.3。AGENTS.md 称 PLAN.md 为「唯一事实来源」，需更新。
2. README 内部矛盾：特性写「零网络权限」，但 AndroidManifest 声明 INTERNET（内容中心下载所需）。
3. Release 构建 `isMinifyEnabled=false`（v1.0 计划「应用加固」前需开启 R8/proguard）。
4. v0.3 Roadmap（turtle/matplotlib/变量动画、AI 助教、错题本 UI）仍 unchecked，但 versionName 已 0.3.3——版本号领先于功能。
5. 已知限制（PLAN 已记录）：settrace 看门狗只对纯 Python 死循环可靠，C 级长调用无法中断。

**未执行项（需用户授权/网络）**：`gradle :app:assembleDebug` 真机构建未在本次跑（耗时+需联网拉依赖），APK 产物未验证。

## 续：修复编译阻断 + 新增电脑端（网页版）

**编译阻断（B-0）已全部修复**：数据层 suspend/事务、`NeonColors`/`NeonTextStyles`/`PythonCodeField` 改为新建定义、UI 导入/签名/`lifecycleScope`、KAPT 恢复（build.gradle.kts 重开 `kotlin-kapt`+`room-compiler`+`kapt{arg room.schemaLocation}`）。`compileDebugKotlin` 经独立验证 0 错误。

**新增电脑端（网页版）`web/`**：用户原要求「电脑端+手机端」双端，但 PLAN.md 仅 Android 单端、电脑端缺失。已建纯前端 + Pyodide 网页：
- `gen_data.py` 把 `lessons_*.json`+`challenges.json` 内联为 `data.js`（`window.LESSONS` 29讲 / `CHALLENGES` 6），把 `runner.py` 内联为 `engine.js`（`window.ENGINE_PY`）+ `REPL_HELPER_PY`；手机端源码零改动。
- 网页端在 Pyodide 内 `runPython(ENGINE_PY)` 后直接调 `run_code_json`/`check_exercise_json`/`repl_exec_json` → 判题/变量快照与手机端 100% 一致。
- 验证：node --check 三 JS 全过；本机 py3.13 直跑 runner.py（run_code/check_exercise pass&fail/看门狗超时）全 PASS；http.server 起 `127.0.0.1:8765` 五资源均 200；Pyodide CDN(jsdelivr) 可达。

**移动端 APK 构建现状**：首次 `assembleDebug` 在 `kaptDebugKotlin` FAILED（55m，疑本机环境块过大致 KAPT worker JVM 无法派生，开局有 `environment too large for exec`）。已用极简 PATH 环境重新后台构建（任务 dVcl5t，日志 build_assemble2.log），结果待定。

**边界**：沙箱无 emulator/镜像/AVD/设备、无图形显示，无法在此渲染 App 界面截图；电脑端浏览器内实跑由用户本机确认。
