# 码上Python —— 赛博朋克风 Python 学习终端

> 产品计划书 v2.0 · 2026-08-26（原名 PyNeon·霓虹派，已更名）
> Slogan：**码上，就是马上。** 英文标识 PY//NOW

---

## 1. 产品定位

一款 **完全离线、内嵌真实 Python 解释器** 的安卓学习 App。
用户在手机上读教程 → 改代码 → 一键运行 → 自动判题 → 升级打怪。

| 维度 | 决策 |
|---|---|
| 目标人群 | Python 零基础入门者 / 通勤碎片时间学习者 / 喜欢黑客美学的年轻用户 |
| 平台 | Android 7.0+（minSdk 24，覆盖约 98% 活跃设备） |
| 分发 | 国内各应用商店 + Google Play（APK/AAB 正常上架，内嵌解释器无合规障碍） |
| 商业化预留 | 免费基础课程 + 高级章节解锁位（本期不做支付） |

## 2. 与市面产品的差异化

竞品：Programiz（干净但平淡）、Sololearn/Mimo（游戏化强但代码执行弱）、Pydroid3（IDE 强但无教学）、QPython（老旧生硬）。

**我们的差异点：**

1. **赛博朋克 HUD 视觉体系**（市面竞品全是白净扁平风）
   - CRT 扫描线、网格底纹、故障字（Glitch）标题、切角霓虹卡片
   - 打字机效果的控制台输出、终端绿荧光 REPL
   - 全局等宽字体气质：整个 App 就是一台"黑客终端"
2. **神经接口 REPL**：像黑客电影一样逐行交互，带命令历史（↑/↓）、一键重置会话
3. **变量快照面板**：每次运行后自动展示命名空间里的变量名/类型/值 —— 把"程序里发生了什么"可视化，竞品均无此能力
4. **赛博等级系统**：脚本小子 → 数据幽灵 → 网络浪人 → 义体黑客 → 街头传奇 → 系统架构师；XP、每日连击 Streak、每日任务
5. **判题式练习**：每课练习内置 assert 测试用例，通过才有 XP，通过动画反馈
6. **100% 离线**：CPython 解释器与课程内容全部打包进 APK，无网可学可练

## 3. 技术选型（已按本机环境锁定）

| 层 | 选型 | 版本 | 说明 |
|---|---|---|---|
| UI | Kotlin + Jetpack Compose (Material3) | Kotlin 2.0.21 / Compose BOM 2024.12.01 | 单 Activity + Navigation |
| 构建 | AGP / Gradle | 8.7.3 / 8.10.2（本机已装） | JDK 17（本机 D:\android-env\jdk-17.0.20+8） |
| Python 引擎 | Chaquopy（MIT 开源） | 16.0.0（支持 AGP 8.6–8.8 ✓） | 运行时 Python 3.13，仅 stdlib，构建期零 PyPI 依赖 |
| 持久化 | DataStore Preferences | 1.1.1 | XP/进度/成就 |
| 内容 | Assets JSON | - | 结构化课程块，渲染层全自定义 |
| ABI | arm64-v8a + x86_64 | - | 真机 + 本机模拟器均可跑 |

SDK 路径：`D:\android-env\android-sdk`（platforms 34/35、build-tools 34.0.0、licenses 已接受）。

### 执行引擎安全设计
- 用户代码运行在独立线程 + `sys.settrace` 死循环看门狗（默认 8 秒超时强制中断）
- stdout/stderr 重定向捕获后回传 UI（前端做打字机动画）
- `input()` 接管：预置输入队列逐行消费，耗尽则提示去 REPL 交互
- 运行结束抓取 globals 快照（过滤双下划线、repr 截断 200 字符、上限 40 个）

## 4. 信息架构（5 Tab 底部导航）

```
启动页 Boot（开机自检打字机动画）
 └─ 主页「指挥台」   等级环/XP、连击、每日任务、继续学习、快捷入口
 ├─ 课程「数据流」   10 课列表（解锁/完成态）
 │   └─ 课程详情     理论块流 + 可运行示例 + 判题练习 + XP 结算弹窗
 ├─ 终端「神经接口」 有状态 REPL，历史记录，重置
 ├─ 挑战「角斗场」   3 个独立编程挑战（同一判题引擎）
 └─ 档案「档案」     统计/成就徽章墙/重置进度
```

## 5. 设计规范（Design Tokens）

| Token | 值 | 用途 |
|---|---|---|
| bg0 | #07090F | 全局底色 |
| bg1 | #0B0F17 | 分区底 |
| surface | #10161F / hi #16202E | 卡片 |
| neonCyan | #00E5FF | 主强调/关键词/进度 |
| neonMagenta | #FF2D78 | 次强调/错误高亮/装饰 |
| neonGreen | #00FF9C | 终端输出/成功 |
| neonYellow | #F7FF00 | 数字/警告/XP |
| textHi/Mid/Dim | #E6F1FF / #8FA3BF / #55647A | 文字三级 |

组件语言：CutCornerShape 切角卡片、1dp 霓虹渐变描边、扫描线叠加、网格背景、Glitch 标题。全局 Monospace 字体。

## 6. 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| M0 | 计划书 + 工程骨架 + 构建链打通 | 本次 |
| M1 | 主题系统 / 执行引擎 / 编辑器 / 数据层 | 本次 |
| M2 | 7 个界面 + 10 课内容 + 3 挑战 + 成就 | 本次 |
| M3 | APK 构建验证 + 模拟器冒烟测试 | 本次 |
| v0.2（后续） | 变量执行过程动画、turtle 海龟画布、matplotlib 图表输出 | 规划 |
| v0.3（后续） | AI 助教（端侧小模型/云混合）、错题本、社区关卡 | 规划 |
| v1.0（上架） | 应用加固、多语言、隐私政策页、商店素材 | 规划 |

## 7. 风险与对策

| 风险 | 对策 |
|---|---|
| Chaquopy 与 AGP/Gradle 兼容 | 已核对官方 changelog：16.0.0 ↔ AGP 8.6–8.8 ↔ Gradle 8.10.2 精确匹配 |
| 构建期访问 PyPI 失败（网络） | MVP 零 pip 依赖，仅用 stdlib |
| 国内网络拉取 Maven 慢/失败 | 仓库链：阿里云镜像 → google() → mavenCentral() 三级回退 |
| 用户死循环卡死 App | settrace 看门狗超时中断（纯 Python 场景可靠） |
| 无限 input 等待 | 输入队列制，耗尽即抛出引导性异常 |

## 8. 验收标准（本次交付即验收）

1. `gradle :app:assembleDebug` 成功产出 APK（观察到的退出码 0 + 产物存在）
2. runner.py / repl.py 通过桌面单元测试（真实运行断言，含超时/快照/判题用例）
3. verify_code（suspects 为空）+ scan_hallucination（blocking=false）
4. 模拟器可安装并启动到主页（尽力项，失败则如实报告）
